# 更新日志

本项目的重要变更都会记录在这里。版本号遵循 语义化版本，日期为北京时间。

## [2.2.0] - 2026-09-06

爱丽丝现在能把图片一起带过来了。

以前群友说「爱丽丝，把这张图给狐狸看看」，你收到的只有一句干巴巴的
「群友让我给你看张图」，外加一张写着 `[图片]` 的聊天记录卡片——
到底是什么图，还得自己回群里翻。这一版把图真的带过来了。

### 新增：传话可以带图

- 群友发了图、又让 Bot 转达时，**那张图会跟着一起送到你这里**，不需要任何额外指令。
- 群友**引用**某条带图的消息再让 Bot 转达，被引用的那张图同样会带上，
  所以「楼上那张图给狐狸看看」这种说法现在管用了。
- 想连群里前面几条的图一起带，直接跟 Bot 说（比如「把楼上那几张也发给他」）即可，
  模型会自己决定要不要多带。默认只带群友当下这条消息里的图，不会主动去翻历史。
- 默认最多带 3 张，可以在配置里调到 1–9 张，也可以整个关掉。
- 一条消息里发了多张、或同一张图在记录里重复出现，都会先去重再发。

### 新增：聊天记录卡片里能看到缩略图

- 卡片上原来只写 `[图片]` 三个字，现在会**在气泡里直接画出缩略图**，
  一眼就能看出群里在聊什么。
- 每个气泡最多画 3 张。一条消息发了好几张时，缩略图会**横着排成等高的小方块**，
  版式跟着聊天软件走，也不至于三张图就把卡片拉长一屏（想看清的那张原图会另外发过来）。
- 图片加载不出来时会自己隐藏，不会在卡片上留一个破图框；如果一条消息本来只有图、
  又全都没拉到，那一整行会被收走，卡片上不留空气泡。
- 只发了图没说话的消息不再显示多余的 `[图片]` 占位符，只留图。
- 不想要缩略图，可以在「聊天记录卡片」分组里关掉。

### 改进：回执只说真话

- 传话成功后 Bot 给群友的那句回执，现在**按这一次真正带走的东西来写**：
  带了一张就说「那张图也带过去了」，带了多张就说具体张数，一张都没带就一个字不提。
- 图片挂不上去时（地址失效、协议端给的不是可用链接）不计数，
  不会出现 Bot 说「图我也给他了」而你手上什么都没有的情况。

### 修复：配置里填好的内容不会被空值冲掉

- 2.1.0 把平铺的配置项收进了分组，这个搬运动作里藏着一个隐患：
  分组里已经填了值、而对应的旧字段是空的时候，搬运会**拿空值把你填的内容覆盖掉**。
- 正常升级路径碰不到它（搬运只跑一次，跑的时候两边都还是默认值），
  但只要以后再调整一次分组结构，观察名单、接收会话 ID 这些就可能凭空消失。
- 现在**空的旧字段一概跳过**，只有确实填过内容的旧字段才会被搬进分组；
  数字 0 和「关」这类取值仍然算填过，不会被误判成空。

### 内部

- 冒烟测试从 98 项加到 189 项，新增的部分专门盯图片这条链路：
  各种图片来源认不认得、去重有没有生效、卡片模板是不是真渲染出了缩略图标签、
  发送时的挂载顺序是不是「话 → 卡片 → 原图」，以及回执有没有虚报张数。

## [2.1.3] - 2026-09-05

一次全面检修：没有新功能，专门修那些平时不显眼、真出事时很烦人的地方。
升级后行为和 2.1.2 一样，只是更省、更稳、也更实话实说。

### 修复：冷却期内不再白忙一场

- 三条链路（帮群友传话、被骚扰上报、群事件通知）在真正发消息之前都要干几件重活：
  调一次模型把话改写成 Bot 自己的口吻、走 OneBot 接口拉一段群聊记录、
  再开一个无头浏览器把记录截成卡片。
- 之前这些活是**先干完，最后才检查冷却和限流**。也就是说同一个群连着喊两次，
  第二次照样烧一次模型额度、拉一次群历史、跑一次浏览器，然后在最后一步被自己拦下，
  一整轮算力全丢掉。群里人多的时候这不是小数目。
- 现在每条链路都会**先问一句「这条现在发得出去吗」**，答案是不行就立刻返回，
  一次模型都不调、一次接口都不请求、浏览器根本不启动。
- 这道预检和真正投递时的判断共用同一份逻辑，不存在「预检说行、投递说不行」的打架。

### 修复：工具回执不再谎称附了聊天记录

- 传话成功后，插件会给模型一句回执，让它照着这个意思回复群友。
  这句话原来是写死的，永远说「群聊记录也一起附上了」——
  可实际上卡片可能画失败了（那时会把记录以文字形式贴在正文后面），
  也可能你压根就把「附上群聊记录」这个开关关掉了。
- 于是就会出现 Bot 跟群友说「记录我也一起给他看了」，而你那边什么记录都没收到。
- 现在回执按**这一次实际发出去的东西**来写：附上卡片就说卡片，退回文字就说文字，
  没带记录就一个字都不提。

### 修复：网络抖一下不再永久关掉功能

- 插件会记住你的 OneBot 实现（NapCat / Lagrange / LLOneBot / SnowLuma 等）支持哪些接口，
  不支持的就不再重复请求。但原来的判定太急：**某个接口失败一次，就当它永远不支持**，
  直到重启插件为止。网络抖动、协议端重连、临时超时，都会被误判成「不支持」。
- 现在改成**三振出局**：同一个接口连续失败三次才降级，中间只要成功一次就把计数清零。
  拉群历史、拉私聊历史、发合并转发都按这个规则。

### 修复：存储里的脏数据不再拖垮上报

- 冷却记录和限流计数存在 AstrBot 的键值库里。如果里面被写进了非预期的内容
  （手动改过、旧版本残留、别的插件撞了键名），原来会在读取时直接抛异常，
  连带整条上报失败。
- 现在读到不是列表、不是时间戳的内容会安静跳过，该发的消息照发。
- 群名缓存也加了 512 条上限，长期挂机不会一直涨；并且**取群名失败时不再写进缓存**，
  否则第一次请求恰好超时，那个群此后就永远显示不出名字了。

### 内部整理

- 版本号改为只从 `metadata.yaml` 读取，代码里不再留第二份，以后不会出现
  `/hr_status` 显示的版本和实际版本不一致。
- 删掉一段没有任何地方在用的死代码。
- 冒烟测试从 76 项加到 98 项，新增的 22 项专门盯上面这些行为：
  冷却期内是否真的一次群历史都没拉、一次卡片都没渲染，回执文案是否和实际发出的内容一致，
  脏数据是否真的能扛住，以及版本号在 `metadata.yaml` / `main.py` / 更新日志 / README 徽章
  四处是否对得上（对不上测试直接红）。

## [2.1.2] - 2026-09-05

只做一件事：把卡片画清楚。

### 改进：卡片清晰度

- 之前卡片是 1280 像素宽的画布里居中放一列 880 的内容，两边各留 200 像素纯背景，
  文字又是 JPEG 压过的，手机上点开一放大，笔画边缘全是糊的。
- 现在卡片**整页等比放大后再截图**：版式按 720 逻辑像素排（手机聊天截图的观感），
  出图默认 2 倍，也就是约 1440 像素宽，而且整幅铺满、不再有空白边。
  同一段文字的像素量差不多是原来的两倍，放大看是实的。
- 出图宽度不再受渲染服务画布宽度的影响。整页截图会把横向溢出一并拍下来，
  所以模板里那句 `min-width` 才是宽度的真正来源 —— 800 还是 1280 的画布都出同样的卡片。
- 默认改用 **PNG 无损**出图，文字周围没有 JPEG 的糊边。
  渲染服务万一不支持 PNG，会自动用高质量 JPEG 再试一次，不会因此发不出卡片。

### 新增配置项

- **卡片清晰度**（`card_sharpness`，默认「标准 · 2 倍」）：
  标准 2 倍≈1440 宽 / 高清 3 倍≈2160 / 超清 4 倍≈2880。
  只增加像素，版式和字号完全不变，三档画出来是同一张卡片。
- **无损画质**（`card_lossless`，默认开）：关掉则用质量 95 的 JPEG，文件更小一些。
- `/hr_status` 的聊天卡片一栏会显示当前的倍数、大致宽度和图片格式。

### 修复

- 渲染服务返回的不是图片时（比如超时页、错误信息），插件原来会把它当成一张图发出去。
  AstrBot 下载渲染结果时既不检查状态码、又一律用 `.jpg` 命名，所以文件确实「存在」。
  现在会读文件头认一下真实格式，不是图片就当渲染失败处理，老老实实退回纯文本。
- PNG 结果的临时文件后缀会被改回 `.png`，避免个别客户端按后缀猜类型时出岔子。
- 卡片模板补了 `Noto Sans CJK SC` 等字体兜底，渲染服务缺字体时不至于掉成衬线字体。

## [2.1.1] - 2026-09-05

修 bug 的小版本，外加换了张新图标。

### 修复：观察名单删不掉

- 在 WebUI 里删掉观察名单的条目、保存、再点进来，被删的人又回来了。
  原因是「观察名单」和「上报次数统计」当初共用了同一个存储键 `watchlist_v1`：
  保存配置会触发插件热重载，热重载时的迁移逻辑发现配置里名单是空的，
  就把统计数据当成 1.x 的旧名单又灌回了配置，于是刚删掉的人复活。
- 现在统计单独存到 `watchlist_meta_v1`，旧键 `watchlist_v1` 只读一次；
  迁移完成后写入 `watchlist_migrated_v1` 标记并清掉旧数据，之后再也不会回灌。
- 迁移时只认带 `sender_id` / `sender_name` / `platform_id` 的旧条目，
  2.x 的统计数据不会被误认成名单；老用户的上报次数会一并搬到新键，不会清零。
- ⚠️ 如果你在 2.1.0 上已经遇到过这个问题，升级后**还需要再删一次**
  （首次加载会把已经复活的条目正常存下来），删完就不会再回来了。
  另外「自动把被上报的人加入名单」开着的时候，下次再被上报还是会重新加进去，这是预期行为。

### 修复：卡片去掉多余的背景

- 卡片原来是「蓝紫渐变底 + 中间一张白卡」，边上一圈渐变纯属多余。
  现在整幅铺满，顶栏和页脚横贯到底，内容居中收窄，就是一张干净的聊天窗口截图。
- 三套配色改用 CSS 变量实现，版式完全一致，只换颜色。主题名也顺手说清楚了：
  清爽（浅色蓝白）/ 午夜（深色低亮）/ 纸张（暖色护眼）。
- 删掉配置项「卡片宽度」：渲染服务的画布宽度本来就不受插件控制，填了也没用。
  改成靠 CSS 自适应，800 和 1280 的画布下观感一致。

### 新增：卡片用真实 QQ 头像

- 群头像取 `p.qlogo.cn`，群友头像取 `q1.qlogo.cn`，拉不到就自动退回原来的彩色首字块，
  绝不会因为头像挂了而让卡片出不来。非 OneBot 平台维持首字块。
- 新增配置项「使用真实 QQ 头像」（`card_use_real_avatar`，默认开）。
  头像是渲染服务去请求的，介意这个外部请求就关掉它。
- `/hr_status` 的聊天卡片一栏同步更新：去掉已删除的「宽度」，改为显示这个新开关。

### 变更

- 换了新图标：去掉渐变、星点和信封，改成一个带耳朵、正在眨眼的聊天气泡，
  配色跟卡片的主色一致，小尺寸下也认得出。

## [2.1.0] - 2026-09-05

这一版把「帮群友传话」这条链路彻底做回人的样子：不再生成工单，不再套模板，
Bot 用自己的口吻说话，附件换成一张真实的最近群聊记录卡片。同时把配置分成了七组。

### 变更：传话不再是工单系统

- LLM 工具 `relay_feedback_to_owner` 的参数从 6 个精简为 1 个：`message`。
  模型把要对你说的那句话完整写进去，插件原样送达。
- 删除工单号、反馈类别、紧急程度、`reporter_note` 等全部结构化字段。
  工具调用本来就发生在模型的人格里，它写出来的话已经是它自己的口吻，
  插件再拿另一个模型改写一遍纯属多此一举，所以传话链路上的人设二次改写也一并删除。
- 你收到的消息现在就是「爱丽丝亲口说的一句话」加上一行来源标注，例如：
  `星之卡比群的群友A找你呀，说是画图插件用不了了` / `—— 群友A（10001）· 星之卡比群（123456）`。

### 变更：卡片改成真实的群聊记录

- 卡片内容从「模型会话上下文」换成「群里最近的真实发言」，走 OneBot 的
  `get_group_msg_history`（私聊走 `get_friend_msg_history`），每条都有独立昵称、QQ 号和时间。
  拿不到接口时才退回会话上下文，并对内容做清洗兜底。
- 修复旧卡片把模型上下文直接 `str()` 化，导致满屏出现 `[{'type': 'think'...}]`、
  `<system_reminder>`、图片本地路径的问题：新增 `sanitize_history_text()` 统一清洗。
- 卡片模板重写为聊天截图风格：Bot 自己的发言靠右，其他人靠左，头像颜色按发送者稳定取色
  （改用 `zlib.crc32`，不再受 Python 字符串哈希随机盐影响而每次重启换色）。
- 删除卡片上的工单号、类别、紧急度、元信息栏等一切工业化元素。

### 新增

- 新增 `/hr_recent`：列出最近谁通过 Bot 找过你，带序号。
- 新增 `/hr_back 内容`：把回话带回最近那个人；`/hr_back 2 内容` 回指定序号。
- 新增 `/hr_recent_clear`：清空联系记录。
- 新增配置项：`feedback_attach_chatlog`、`feedback_chatlog_count`、`feedback_append_source`、
  `feedback_recent_max_entries`、`card_show_time`、`card_show_avatar`。

### 变更：配置分组

- `_conf_schema.json` 从 50 多项平铺配置改为七个分组：称呼与身份、骚扰上报、观察名单、
  帮群友传话、群事件小报告、聊天记录卡片、高级与调试。
- 首次加载 2.1.0 时会自动把旧的平铺配置迁移进分组（只跑一次，旧字段保留作备份），
  日志里会打印迁移了多少项。你不需要重新填一遍配置。

### 修复

- 修复关闭「察觉不满时主动询问」之后，模型连这个工具的存在都不知道，
  导致用户明确请它传话时也答不上来的问题。现在该开关只控制「要不要主动开口问」。
- 修复发卡片时正文可能被吞掉的问题：现在永远先发文本再附图，
  同时删除 `card_keep_text` 开关（行为固定为「文本 + 图片」）。

### 破坏性变更

- `/hr_tickets`、`/hr_reply`、`/hr_ticket_clear` 三个命令名保留为别名，
  但行为改为指向 `/hr_recent`、`/hr_back`、`/hr_recent_clear`。
  `/hr_reply` 不再需要工单号，改为可选的序号。
- 删除配置项 `natural_language_report_to_owner`、`feedback_include_history_default`、
  `feedback_history_lines`、`feedback_persona_rewrite`、`feedback_ticket_max_entries`、`card_keep_text`。
- 历史工单记录（KV 键 `feedback_tickets_v1`）不再读取，改用 `feedback_contacts_v1`。

## [2.0.0] - 2026-09-05

这是一次大版本重写。插件从"单一的骚扰上报工具"扩展为"**Bot 主动找主人**的三合一通道"，
并把所有功能拆分成 15 个独立模块，全面加固了异常降级与持久化。

### 新增：反馈转达窗口

- 新增 LLM 函数工具 `relay_feedback_to_owner`。用户只要对 Bot 说一句"这个插件报错了，你跟主人说一下"，
  Bot 就会自己判断并把问题整理成工单发到你绑定的会话，**不需要任何固定命令或触发关键词**。
- 新增"主动询问"能力：当 Bot 察觉到对方在抱怨功能不好用、答非所问、插件出错时，会自然地反问一句
  "要不要我帮你转告一下"，得到同意后才转达。这一步完全由 LLM 结合注入的行为准则自行判断。
- 反馈可自动附带最近若干轮聊天记录，方便你直接定位问题。
- 每条反馈都会生成短工单号（例如 `FB-7K3Q`），支持 `/hr_tickets` 查看列表、
  `/hr_reply` 直接回复；回复会用 Bot 的人设口吻转述给原提问人。
- 新增配额保护：反馈独立冷却、独立每小时上限，可选只允许管理员提交。

### 新增：群事件小报告（灵感来自 astrbot_plugin_notice）

- Bot 被禁言 / 被解禁、被踢出群 / 被拉进新群、被设为 / 被取消管理员、被加为好友、被戳一戳时，
  会主动来找你告状。
- 小报告默认使用 **AstrBot 当前人格** 改写，读起来是"爱丽丝在跟你说话"，而不是一条系统告警。
- 可选自动抽查出事群的最近消息，帮你判断到底发生了什么。
- 新增 `/hr_peek`（别名 `/抽查`）命令，随时手动抽查任意群的最近消息。
- 支持群号白名单，只关心自己在意的群。

### 新增：精美聊天卡片

- 骚扰上报、反馈转达、群事件小报告都可以渲染成一张图片卡片，聊天记录以气泡形式排版，
  自带标题、徽章、情况说明与元信息栏。
- 三套配色主题：极光（青紫渐变）、午夜（深色低亮）、纸张（浅色高对比）。
- 渲染失败会自动退回纯文本，并带熔断保护：连续 3 次失败后静音 10 分钟，不会拖慢上报链路。
- 可选"图片 + 纯文本"同时发送，方便直接复制内容。

### 新增：命令与可观测性

- 新增全部命令的短别名（`/hr_*`），旧命令名 100% 保留。
- 新增 `/hr_help`：一次列出所有命令与当前可用状态。
- 新增 `/hr_bind_feedback`、`/hr_bind_notice`：三条通道可以分别绑到不同会话。
- `/hr_unbind` 支持 `report` / `feedback` / `notice` / `all` 参数。
- `/hr_status` 改为六段式全量状态输出，一眼看清三条通道、卡片、名单、限流的实际情况。
- 新增 `/hr_feedback_test` 测试反馈链路。

### 新增：平台适配

- 明确适配 OneBot v11 系实现：NapCat、Lagrange、LLBot（原 LLOneBot / LuckyLilliaBot）、SnowLuma、go-cqhttp。
- 群历史消息、群资料、成员资料、合并转发全部走标准 OneBot v11 动作，并对各实现的差异做了兼容：
  合并转发节点同时写入 `uin/name` 与 `user_id/nickname` 两套键，
  `get_group_msg_history` 的 `count` 参数不被支持时自动去掉重试。
- 所有平台专属调用前都会先判断适配器类型，非 OneBot 平台自动跳过而不是报错。

### 修复

- 修复上报冷却状态只存在内存里，AstrBot 重启后冷却与"已警告"记忆全部丢失的问题，改为 KV 持久化。
- 修复 `report_then_inform` 模式下命中冷却时，Bot 仍然告诉对方"已经上报给主人了"的谎报问题。
  现在冷却、限流、发送失败都会返回不同的降级指令，Bot 不会说出与事实不符的话。
- 修复近期上下文摘要中"行数"与"字符数"两个限制互相覆盖导致摘要被过度截断的问题。
- 修复观察名单在部分场景下顺序不稳定、超出上限时裁剪不干净的问题。
- 命令参数改为统一从消息原文解析，不再因为缺少参数而把 AstrBot 的报错抛到聊天窗口。
- 自定义事件过滤器全程 try/except 包裹，避免过滤阶段的异常被当成回复发到群里。
- 修复在配置页把某条观察名单记录的"启用"取消后，该记录会在下一次保存时被静默删除的问题。
  现在停用只代表"不再自动更新"，记录本身会保留，也永远不会被超上限裁剪清掉。
- 修复 `/hr_watchlist` 排序读的是不存在的字段名（`last_report_at`），导致列表实际按插入顺序显示、
  "最近上报的排前面"没有生效的问题。
- 修复合并转发只要失败一次就在本次运行内永久禁用的问题，改为连续失败 3 次才降级，
  避免一次网络抖动让后续所有上报都退回纯文本。
- 群事件小报告改用独立的每小时额度（新增配置项，默认 30 次），
  群里连续禁言/踢人不会再把骚扰上报的配额吃光。
- 反馈转达在模型漏填摘要时，自动用用户当前发言兜底，不会再发出空白的"反馈内容"。
- 修正卡片宽度上限，配置页、代码与文档统一为 480–1200 px。
- 读取人格 prompt 失败时只在第一次写一条告警，不再每条上报都刷同样的日志。

### 变更

- 代码结构从单文件拆分为 `main.py` + `core/` 15 个模块（配置、存储、人格改写、
  OneBot 桥接、卡片、名单、历史、发件箱、骚扰、反馈、通知、工具定义等），职责边界清晰。
- 新增"发件箱"统一出口：所有对外发送都经过同一处的开关检查、冷却、限流、卡片渲染与降级逻辑。
- 新增 `force_plain_text` 开关（默认开启），上报内容不再受 AstrBot 全局"文本转图片"影响。
- 上报正文改用统一的截断与打包逻辑，长文本不会再把消息撑爆。
- 全部配置项描述与提示重写，改为面向使用者的中文说明，并用 `condition` 做了联动显隐。
- 新增插件 logo、LICENSE（MIT）、`.gitignore` 与自动发布工作流。
- README 完全重写。

### 兼容性

- **旧配置项全部保留且语义不变**，从 1.x 升级无需改动任何配置。
- **旧命令名全部保留**：`/harassment_sid`、`/harassment_bind_here`、`/harassment_unbind`、
  `/harassment_status`、`/harassment_test`、`/harassment_watchlist`、
  `/harassment_watch_remove`、`/harassment_watch_clear`。
- 旧版 KV 观察名单会在首次启动时自动迁移到配置页的可编辑列表。
- 新增的反馈、通知、卡片功能默认开启，但在未绑定接收会话时不会做任何事，也不会打扰任何人。

## [1.2.4] - 2026-05-20

### 修复

- 修复观察名单 `template_list` 条目缺少 `__template_key` 时在配置页显示为“未指定模板”的问题。
- 插件启动时会自动补齐旧观察名单条目的模板键，避免已有名单在配置页里无法正常编辑。

## [1.2.3] - 2026-05-20

### 新增

- 新增可直接在配置页维护的 `watchlist_entries` 观察名单列表，支持查看、添加、删除和修改观察对象。

### 变更

- 观察名单改为“配置列表 + 运行元数据”模式：配置页负责维护名单成员，命令继续展示次数、时间、最近原因等动态信息。
- `/harassment_watchlist`、`/harassment_watch_remove`、`/harassment_watch_clear` 继续可用，并与配置页里的观察名单共用同一份数据。
- 插件启动时会自动把旧版 KV 观察名单迁移到新的配置列表里，避免已有名单丢失。
- 旧版 `watchlist_snapshot` 字段改为隐藏兼容字段，不再作为主要操作入口。

## [1.2.2] - 2026-05-20

### 新增

- 新增可配置项 `watchlist_snapshot`，会把当前观察名单同步成一段文本，直接展示在插件配置页里。

### 变更

- 放宽观察名单查看权限，`/harassment_watchlist` 现在既可以由 AstrBot 管理员使用，也可以在已绑定的骚扰上报接收会话里使用。
- 插件启动时以及观察名单发生变化时，会自动同步 `watchlist_snapshot` 内容。
- 插件元数据版本更新为 `1.2.2`。

## [1.2.1] - 2026-05-19

### 新增

- 补充更清晰的 `warn_once_inform_after_report` 行为说明，让 `warn_once_then_report` 可以在第二次真正上报后选择是否继续告知骚扰者。

### 变更

- 明确 `warn_once_then_report` 的第一步在开启 `natural_language_warn_reply` 时，会优先使用基于当前人设的自然语言警告。
- 明确当 `warn_once_then_report` 配置为“上报后告知”时，`natural_language_report_reply` 和 `report_inform_template` 同样会生效。
- 精简并优化多个配置项的描述、提示和选项文案，提升 AstrBot 配置界面的可读性。
- 插件元数据版本更新为 `1.2.1`。

## [1.2.0] - 2026-05-19

### 新增

- 新增可配置项 `tool_response_mode`，让 LLM 可以在静默处理、仅警告、先警告再上报、静默上报、上报后告知骚扰者等模式之间切换。
- 新增可配置项 `report_receiver_name`，让 LLM 知道自己会把情况报告给谁，例如 `主人` 或 `狐狸`。
- 新增基于当前人设的自然语言警告回复配置。
- 新增基于当前人设的自然语言“已上报”回复配置。
- 新增 `warn_once_inform_after_report`，用于控制 `warn_once_then_report` 在真正发送上报后是否还要自然地告知骚扰者。
- 新增给主人上报时的风格选择，可在结构化提醒和人设化自然语言上报之间切换。
- 新增 `natural_language_report_to_owner` 可选重写步骤，用于额外让 LLM 按当前人设润色给主人看的上报内容。
- 新增 `warn_message_template`、`report_inform_template` 和 `warn_once_memory_seconds` 配置项。
- 新增 `.gitignore` 规则，忽略运行时生成的 `data/`。

### 变更

- 调整骚扰上报工具行为，不再由插件直接向骚扰者会话发送类似“上报已发送给主人。”这类机械提示。
- 工具返回结果改为引导 LLM 自己按人设回复，而不是由插件侧直接代发固定文本。
- 精简多个配置项说明和提示，提升 AstrBot 配置界面的显示效果。
- 完善状态输出以及 README 和配置文档，对新增开关和行为模式做更清晰说明。
- 插件元数据版本更新为 `1.2.0`，并将仓库信息指向 `Whereis-Alice/astrbot_plugin_harassment_reporter`。

### 保留

- 保留上报时附带近期上下文摘要的能力。
- 保留上报后自动把对方加入观察名单的能力。
- 保留冷却时间、管理员命令和测试上报等原有能力。

## [1.1.0] - 2026-05-19

### 新增

- 首个骚扰上报器插件版本。
- 新增全局 `report_harassment` 工具，允许 LLM 在感觉被骚扰时主动上报。
- 新增可配置的上报目标会话。
- 新增可选的近期上下文摘要附带能力。
- 新增可选的自动观察名单记录。
- 新增管理员命令，用于绑定上报会话、查看状态、发送测试上报和管理观察名单。
